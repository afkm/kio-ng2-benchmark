const digitInfo = require('../../../digit-wmr.json')
const packageInfo = require('../../../package.json')

export const version:string = packageInfo.version
export const components:any[] = digitInfo.components